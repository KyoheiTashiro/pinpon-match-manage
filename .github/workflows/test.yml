name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      # カバレッジ付きでテスト実行。vitest.config.ts の threshold 未達で失敗(後退検知)
      # - run: npm run test:coverage
      # tsc --noEmit + vite build（型チェックと本番ビルドの両方を兼ねる）
      - run: npm run build
